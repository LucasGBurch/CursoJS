var c = 1
// Laços de repetição tipo while (teste lógico no início)
while(c <= 6) {
    console.log(`Mensagem ${c}`)
    c++
}

// Tipo do {} while (teste lógico no fim)
c = 0 // reiniciando em 0
do {
    console.log(`Mensagem ${c}`)
    c++
} while(c <= 5)